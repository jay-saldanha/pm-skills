# sardine-kb → risk-docs topic map

> **Last updated:** PENDING (generated on first run of `/risk-docs-drift`)
> **Notion drift report page ID:** PENDING

This file is built by `/risk-docs-drift` on first run and updated by the user or by subsequent runs. See the skill's Step 2 for the derivation logic.

## Mapping

| KB doc | KB summary (excerpt) | risk-docs page(s) or section | Externally relevant? | Notes |
|---|---|---|---|---|
| _empty_ | _Run `/risk-docs-drift --dry-run` to generate this table_ | | | |

## Instructions for future runs

- Add new KB docs as rows when sardine-kb is updated (bump version → new doc → add row here).
- When a new risk-docs section is added, check whether it maps to an existing KB doc; add/update the row.
- `partial` rows: read the KB doc's inline `internal only` / `Sardine-internal only` markers to decide which sections feed external proposals. Those sections are excluded by the skill's Step 5a pre-gate.
- `snapshot`-tagged KB docs (keyword `snapshot` in frontmatter): findings from these docs are flagged with a staleness warning; verify the cited `sardine-all` line before treating the KB as authoritative.
- The `Notion drift report page ID` above is populated on first full (non-dry-run) execution. Do not edit that line manually.
